docker start kms-pgvector
source venv/bin/activate
python manage.py runserver
npm run dev

minerU

- từ cái file cô đưa chưa biết được có thể lấy ra những thông tin gì
    các thông tin sẽ là những nội dung bên trong bài giảng chứ không gói gọn lại ở tiêu đề
    không biết thì sẽ không fit được với giao diện

- chủ đề ít

- Hiếu nghĩ cái thứ 2 nó khó xây dựng hơn cái thứ 1
    tại vì thiếu chuyên môn để hiểu

- reload không mất login => xong
- trong cái card, khi bấm vào tên người đăng thì xuất hiện hộp thoại giới thiệu thông tin về người đó => xong
- Những cái nào có sự xuất hiện của thư mục thì nên trình bày theo cấu trúc cây thư mục => xong
- trong phần xét duyệt bài giảng hãy hiển thị file word đó trong trình duyệt và những thông tin về bài giảng đó => xong
- Xây dựng chức năng bình luận, đánh giá cho 1 file giáo án dựa trên database đã xây dựng => xong
- Xây dựng lại giao diện khi mà mở card, mở thì phải mở full màn hình sao khi mở full màn hình thì màn hình sẽ được chia làm 2 khu 1 khu để hiển thị các bình luận đánh giá, khu còn lại để hiển thị bài giảng và các thông tin khác của bài giảng => xong
- Xây dựng chức năng phân trang, mỗi trang từ 10 đến 20 tài liệu, các tài liệu có thể được sắp xếp theo ngày tải, số sao, số lượt đánh giá => xong
# - nâng cấp giao diện Quản trị người dùng & Phân quyền hệ thống cho chuyên nghiệp hơn khi mà hệ thống có cả nghìn người dùng
- Xây dựng giao diện chỉnh sửa thông tin của cá nhân của người dùng cho chuyên nghiệp => xong
- Khi bấm vào biểu tượng (hình ảnh của phần hệ thống quản lý tri thức) thì hãy quay về tab thư viện chung => xong
- sắp xếp đánh giá bằng sao => xong (đã có sẵn bộ lọc "⭐ Đánh giá cao nhất / ⭐ Đánh giá thấp nhất" trong dropdown Sắp xếp của Thư viện chung)
- tài liệu của thư viện cá nhân khi publish sẽ không bị mất => xong (đã chỉnh sửa API Đề xuất để không làm sạch directories cũ, giữ nguyên liên kết thư mục cá nhân)
- Đồng bộ hiển thị ở tab thư viện cá nhân và thư viện chung cho giống nhau => xong
- Sắp xếp và bộ lọc theo số sao, số lượt đánh giá ở thư viện chung => xong
- Hiển thị số sao trung bình và số lượt đánh giá ở ngoại quan card (cả Thư viện chung và Thư viện cá nhân) => xong
- Thêm bảo mật chống Code Injection (XSS) cho phần bình luận bằng cách lọc mã độc (HTML Escape) ở Backend => xong
- Xây dựng bộ phân tích giáo án tự động (.docx) bóc tách metadata (tiêu đề, môn học, lớp, thời lượng, đối tượng, hoạt động, từ khóa) ở Backend và tự động điền (Auto-fill) form tải lên ở Frontend => xong
- Xây dựng bộ chuyển đổi giáo án Word sang Markdown tuần tự và lưu trữ tự động vào cơ sở dữ liệu (PostgreSQL) phục vụ RAG => xong
- Sửa lỗi hiển thị và tích hợp cơ chế bóc tách động dự phòng (on-the-fly fallback) cho các giáo án mẫu cũ => xong
- Xây dựng nút chuyển đổi chế độ xem tài liệu (Bản Word gốc vs Bản Markdown) ngay trên giao diện => xong
- Xây dựng bộ kết xuất bảng biểu và tiêu đề Markdown cao cấp sang HTML trên giao diện => xong
- Xây dựng cơ chế chống tải tài liệu trùng lặp (trùng tiêu đề hoặc trùng lặp nội dung đến 95%) sử dụng thuật toán difflib SequenceMatcher tối ưu hóa hiệu năng cực cao => xong
- Cập nhật cơ chế chống trùng lặp theo scope (Công khai / Cá nhân), nâng cấp UI/UX hiển thị thông báo trùng lặp dạng card tương tác cho phép "Chỉnh sửa lại" hoặc "Xem tài liệu đã có" tức thì => xong
- Ẩn hoàn toàn cột bình luận & đánh giá khi mở xem tài liệu cá nhân (LOCAL) chi tiết và mở rộng cột hiển thị nội dung thông tin ra 100% chiều rộng màn hình để tối ưu hóa không gian => xong
- Tích hợp kiểm tra trùng lặp tự động ngay khi vừa nhấn nút "Đề xuất công khai", hiển thị cảnh báo đỏ và nút xem tài liệu trùng lặp lập tức trước khi bắt chọn thư mục => xong
- Nâng cấp Tìm kiếm thông minh (PostgreSQL Full-Text Search gộp Vector & điểm Rank) và Bộ lọc nâng cao dạng Hybrid (sidebar Multi-select Checkbox lớp học JSONField + topbar Popover bộ lọc nhanh) => xong
- Tinh chỉnh và hợp nhất thanh tìm kiếm đa nhiệm ngữ cảnh (Unified Contextual Search) cho cả 3 tab Thư viện chung, Cá nhân và Lịch sử => xong
- Thiết kế lại góc thông tin cá nhân (name card): căn giữa đối xứng hoàn hảo, xóa nút Cá nhân thừa, click trực tiếp vào tên/vai trò để chỉnh sửa, thêm hiệu ứng di chuột bút chì absolute không làm xê dịch chữ => xong
- Loại bỏ các bài giảng rác thiếu tệp tin và seed lại 9 bài giảng mẫu chuyên sâu phủ kín 100% tất cả các thư mục con trong hệ thống => xong
- Tinh giản hiển thị thư mục trong Card bài giảng: dùng thuật toán lọc nút lá, chỉ hiển thị 1 đường dẫn chi tiết duy nhất (ví dụ Vật lý / Sóng dừng) thay vì hiện lặp cả thư mục cha => xong
- Sửa lỗi đếm trùng lặp (double-counting) số lượng bài giảng trong cây thư mục bằng cơ chế lọc ID độc bản => xong
- Khắc phục triệt để lỗi hiển thị lặp lại các bài giảng ở cả thư mục cha và thư mục con, tự động ẩn bài giảng ở thư mục cha nếu nó đã được xếp vào các thư mục con cụ thể => xong
- Thiết kế và tích hợp component LessonActivitiesTimeline hiển thị tiến trình dạy học dạng Vertical Timeline scannable, thoáng mắt dưới phần thông tin tổng quan và trên phần preview tài liệu; tự động ẩn an toàn nếu không có dữ liệu => xong
- Thắt chặt khử trùng lặp nội dung khi Đăng trực tiếp (Direct Publication/Upload) cho Admin và Giáo viên có quyền => xong
- Phân chia Phân quyền thư mục thành 2 sub-tab: Thư mục cá nhân (chỉ xem thư mục riêng) và Thư mục public (tích chọn phân quyền cho giáo viên/admin, ẩn đối với thành viên) => xong
- Thêm chức năng Khóa / Mở khóa tài khoản người dùng trực tiếp trên Dashboard Admin => xong
- Gộp giao diện chỉnh sửa nhận xét của bản thân inline trực tiếp vào phần hiển thị Nhận xét của bạn, tự động hiển thị chế độ chỉnh sửa hoặc chế độ xem động => xong
- Xây dựng biểu đồ thống kê phân bổ số sao trực quan (Rating bar chart) và bộ lọc số sao (Star rating pills filters) tương tác động trong phần nhận xét giáo án => xong
- Tích hợp thanh cuộn mỏng (scrollbar-thin) giới hạn chiều cao tối đa cho cây thư mục trong cả 2 tab Thư viện chung và Thư viện cá nhân, đồng thời mặc định chỉ tự động mở rộng đến cấp 2 => xong
- Triển khai AI Processing Hub với Tiến trình Roadmap 5 bước xử lý tri thức thời gian thực => xong
- Xây dựng cấu hình phân mảnh thông minh (Heading/Fixed Strategy) và AI Model Engine settings cho Admin => xong
- Tách biệt hành vi click card xem chi tiết với nút kích hoạt và liên kết trợ lý AI tập trung => xong
- Hỗ trợ tải lên và hiển thị trực tiếp tài liệu Markdown (.md) và văn bản thô (.txt) => xong
- Khắc phục triệt để lỗi ghi log Unicode CP1252 trên Windows khiến treo luồng ngầm => xong
- Khắc phục lỗi crash gửi tin nhắn chatbot AI do biến is_asking_about_focus chưa khởi tạo (UnboundLocalError) khi bắt đầu chat không chọn tài liệu focus => xong
- Đồng bộ hệ thống thuộc tính giáo án trải nghiệm Sinh học (4 Mạch kiến thức, 10 Chủ đề, 2 Đối tượng HS, 24 Kiến thức sinh học liên quan) cho bộ lọc Sidebar, Topbar Popover, trang UploadPage, Edit Modal và API Views backend => xong
- Khắc phục lỗi crash luồng ngầm GGUF song song bằng Thread Lock bảo vệ và tăng n_ctx an toàn lên 4096 => xong
- Sửa lỗi hiển thị sai lệch đồ thị bằng cách làm sạch an toàn tham số lesson_id query params ở Backend => xong
- Nâng cấp giới hạn thực thể lên tối đa 12 thực thể và tối ưu prompt bóc tách đa mục tiêu khoa học & sư phạm => xong
- Tách biệt hành vi mở chatbot AI khi xem tài liệu và hỗ trợ tự động bind ngữ cảnh tài liệu focus khi hỏi đáp => xong
- Thiết kế Tab WikiNotes cấp cao mới với Split-pane hiện đại và trình phân tích WikiLinks Obsidian [[Tag]] tương tác => xong
- Tạo signal dọn dẹp pre_delete và script quyét dọn vault tự động xóa note mồ côi/liên kết đứt khi xóa tài liệu => xong
- Tối ưu hóa dung lượng ngữ cảnh Graph RAG (loại bỏ danh mục hệ thống khi ở chế độ FOCUS_QA) giúp loại bỏ triệt để lỗi vượt quá context window 4096 tokens, đảm bảo chạy Qwen 7B GGUF offline cực kỳ mượt mà => xong
- Thiết kế lại và tổng quát hóa toàn bộ Prompt của AI và các bộ lọc từ khóa chéo chéo (subject-agnostic) để hỗ trợ toàn bộ các môn học và Hoạt động Trải nghiệm & Hướng nghiệp tổng quát, thay vì bị giới hạn hay định kiến cứng về Sinh học => xong
- Khắc phục lỗi và tự động phân tách tệp tin Word (.docx) ngầm để đồng bộ bản Markdown chi tiết thời gian thực cho các giáo án mẫu cũ phục vụ Sơ đồ tư duy tương tác => xong
- Nâng cấp bộ giải mã thích ứng (Adaptive Parser) trên Frontend hỗ trợ đa dạng cấu trúc giáo án (dạng bảng và danh sách tự do), tự động đồng bộ chéo nhánh Tiến trình & Hoạt động và bảo đảm 4 nhánh Sơ đồ tư duy luôn hiển thị đầy đủ thông tin thực tế => xong
- Tích hợp nút điều khiển "🎯 Về giữa" (Center View) cho Sơ đồ tư duy tương tác sử dụng React Flow fitView với hiệu ứng chuyển cảnh mượt mà thời lượng 800ms giúp tự động căn chỉnh sơ đồ cân đối => xong
- Chỉnh sửa giao diện của Chat AI: Di chuyển nút ẩn/hiện lịch sử sang bên trái (trong sidebar) và tự động ẩn/hiện bằng tab có nút "▶" ở cạnh trái khung chat khi sidebar đóng => xong
- Cho phép người dùng kéo qua lại (resize) giữa thanh lịch sử chat và khung chat bằng thao tác mouse drag trên dải phân cách, đồng thời lưu trữ độ rộng động vào localStorage => xong
- Khắc phục triệt để lỗi icon AI chat nổi che đi nội dung của nút "Tải tài liệu về máy" ở footer modal chi tiết bằng cơ chế vị trí bottom động (bottom: 96px khi xem chi tiết modal và bottom: 24px khi ở trang chủ) => xong
- Tích hợp nút 🛑 Dừng câu trả lời (Stop Response) sử dụng AbortController để dừng tạo câu trả lời và giải phóng UI ngay lập tức => xong
- Tích hợp tính năng 🔄 Làm lại câu trước (Remake previous question) giúp thu hồi lượt hội thoại cuối cùng và đưa lại câu hỏi cũ vào khung input để người dùng chỉnh sửa => xong
- Nâng cấp bộ phân tích và hiển thị tin nhắn chatbot AI hỗ trợ render Markdown phong phú tự động bao gồm Tiêu đề, danh sách, và bảng biểu (|) có định dạng sắc nét mà vẫn bảo toàn 100% các liên kết bài giảng mẫu => xong
- Tích hợp tính năng chỉnh sửa trực tiếp câu hỏi cũ (Inline Prompt Editing): Người dùng có thể click biểu tượng bút chì ✏️ bên cạnh tin nhắn của mình để sửa trực tiếp trong khung nhập, tự động cắt lịch sử từ thời điểm đó và gửi lại truy vấn mới => xong
- Cải thiện giao diện trong card bài giảng chi tiết: Thiết kế lại toàn bộ phần xem tài liệu và sơ đồ tư duy rườm rà thành 2 thẻ nút tiện ích cao cấp (Interactive Portal Grid) mở rộng sang các trang không gian làm việc toàn màn hình (Dedicated Workspace Pages) màu sáng sang trọng. Sơ đồ tư duy được tái cấu trúc thành dạng Sơ đồ 2 bên đối xứng cân đối hoàn hảo (Bilateral Symmetrical Layout) tự động zoom to gấp 2 lần cực thoáng và sắc nét. Trang xem tài liệu chi tiết ưu tiên hiển thị định dạng trích xuất Markdown tinh gọn và hỗ trợ bộ đôi nút tải xuống thông minh cho cả bản gốc Word (.docx) lẫn bản sao Markdown (.md) trùng khớp hiển thị web; đồng thời ô cửa sổ chi tiết lá sơ đồ (DetailModal) được nới rộng tối đa lên 1140px chia làm 2 cột song song với cỡ chữ phóng to 14.5px cực dễ quan sát và loại bỏ trùng lặp nhãn => xong
- Sửa lại role admin: có quyền chỉnh sửa thông tin các file người khác tải lên (kể cả các admin khác) => xong
- Sơ đồ tư duy: Tích hợp bảng xem nhanh thông tin chi tiết (Floating Live Preview Panel) hiển thị bự ra ngay GIỮA màn hình khi rê chuột (hover) vào bất kỳ lá sơ đồ nào, loại bỏ hoàn toàn lỗi nhấp nháy (flicker) bằng lớp phủ trong suốt (pointer-events: none), cho phép người dùng đọc trực tiếp toàn bộ Nội dung chi tiết & Định hướng sư phạm CTGDPT 2018 tức thì với hiệu ứng scale-in cực mượt mà => xong

- Ngày 2/6/2026
- Thiết kế lại logic chỉ cập nhật (load lại những file có sự thay đổi) ở Frontend và Backend (Phương án C: Hybrid state sync + lightweight serialization), loại bỏ triệt để tình trạng tải lại toàn bộ database khi thêm, sửa, xóa, duyệt, đánh giá => xong
- Backend: bổ sung LessonPlanListSerializer (bỏ cột nặng content_preview khỏi API danh sách, chỉ tải khi xem chi tiết) => xong
- Frontend: chuyển toàn bộ bộ lọc sidebar checkbox và topbar popover sang xử lý 100% client-side (useMemo), bấm lọc không gửi bất kỳ request nào lên server => xong
- Sửa lỗi bộ lọc Lớp: thay free-text bằng dropdown checkbox chuẩn (không lọc bằng text nữa) => xong
- Chuẩn hóa giá trị target_student ("Học sinh thành thị", "Học sinh nông thôn") trong CSDL để lọc chính xác 100% => xong
- Logic bộ lọc chọn nhiều: AND mode (chọn nhiều giá trị thì card phải thỏa TẤT CẢ điều kiện) => xong
- Sơ đồ tư duy: loại bỏ tính năng hover xem nhanh (Floating Quick Preview) gây phiền nhiễu => xong
- Session-level ratings cache: lần đầu mở card mới fetch từ server, lần sau trong cùng phiên đọc thẳng từ cache (0 request) => xong
- Sau khi gửi / sửa đánh giá: cập nhật ratingsCache cục bộ, loại bỏ hoàn toàn GET /ratings/ thứ 2 sau POST => xong
- Sắp xếp theo đánh giá: dùng total_ratings làm tiebreaker khi cùng số sao (nhiều đánh giá hơn ưu tiên hiển thị trước) => xong
- Lọc WikiNotes theo bài giảng cụ thể (chỉ hiển thị các ghi chú và khái niệm liên quan đến tài liệu đang focus) => xong
- Lọc khoảng cách liên kết (BFS Hop Distance: 1, 2, 3 hops) trên đồ thị tri thức => xong
- Định nghĩa khái niệm (Concept Notes) học thuật tự động bằng LLM thay cho mô tả mặc định cũ => xong
- Cho phép người dùng tùy chọn cấu hình model (3b, 7b, api) để sinh Concept Notes theo thiết lập cá nhân => xong
- Hợp nhất tất cả các tài liệu kỹ thuật .md thành 2 file duy nhất: he_thong.md và he_thong_rag_llm.md => xong
- Sửa lỗi click đúp mở giáo án trên Đồ thị tri thức (Graph Canvas) => xong
- Tích hợp nút Dừng xử lý (Stop Task) cho luồng ngầm AI RAG => xong
- Thiết lập cơ chế Beacon tự động dừng toàn bộ tiến trình khi người dùng tắt/thoát web => xong
- Tách biệt hành vi click card xem chi tiết với nút kích hoạt và liên kết trợ lý AI tập trung, sửa lỗi hộp thoại "Lịch sử trò chuyện" (Chat history dialog) xuất hiện sai ngữ cảnh ở trang chủ => xong
- Sửa lỗi click/double click trên Đồ thị tri thức (Graph Canvas) không mở được giáo án khi giáo án không nằm trong cache local => xong
- Khóa hoàn toàn lỗi Zoom Đồ thị kéo theo Zoom Trang web và loại bỏ hiện tượng cuộn kép (Scroll Chaining) bằng overscroll-behavior: contain => xong

- Thứ 1 là không có 2 file mẫu cho 2 đối tượng học sinh thành thị/nông thôn (chuyên biệt cho từng đối tượng)
- Thứ 2 là do đã thống nhât từ trước là 1 file đưa lên hệ thống sẽ là file KH bài giảng hoàn chỉnh 
    tại vì đầu tiên với nhu cầu là truy vấn tìm kiếm giáo án cho nhanh, tìm kiếm giáo án theo mô tả của ngta thì cần có file KHBG hoàn chỉnh
    việc tụi em làm là đọc file trích xuất thông tin để đáp ứng nhu cầu truy vấn nên file phải chi tiết hóa một chút
    
    Trong trường hợp cô muốn tải 1 file với cấu trúc theo kiểu so sánh giữa 2 đối tượng học sinh thì
        - Nó sẽ giúp ích được gì cho người sử dụng tài liệu của cô?
        - Giả sử cô chỉ đưa ra ý tưởng thì không thể làm được.


- vẽ 1 form cho gv cấp 3 chọn nhiều thứ, đối tượng HS, chọn phòng thiết bị
chọn thời gian, phải vẽ hết (cô nói trong ghi âm) các sự lựa chọn của GV
khi mà họ chọn thì bấm nút tìm nó ra cái gì?
- cô upload từng file phải lấy được nhiều thông tin

- Khóa cứng lỗi zoom đồ thị và zoom toàn trang, đồng thời tích hợp cơ chế ngăn chặn cuộn kép (overscroll-behavior) => xong
- Tránh đóng chatbot tự động khi xem chi tiết giáo án hoặc quay lại để người dùng luôn có thể click Quay lại điều hướng từng bước => xong
- Sửa hành vi double-click vào Thư mục (directory) trên đồ thị để mở/chọn thư mục đó trong cây thư mục thư viện ở trang chủ => xong
- Ghim bảng tóm tắt thực thể/bài giảng/thư mục lệch khỏi nút gốc để tránh che khuất, kiểm tra giới hạn viền đồ thị và hỗ trợ kéo co giãn từ cả 4 góc của bảng, tự động phóng to/thu nhỏ kích cỡ chữ trong popup tương ứng với tỉ lệ co giãn => xong
- Thêm nút 'Quay lại' trên thanh tiêu đề chi tiết giáo án (App.tsx) bên cạnh nút close ✕ để quay lại tài liệu đã xem trước đó theo từng bước mà không cần mở chatbot, tự động lưu trữ ngăn xếp lịch sử xem giáo án => xong
- Sửa lỗi useRef is not defined ở App.tsx bằng cách thêm import useRef từ react => xong
- Đồng bộ tự động phiên chatbot AI khi di chuyển hoặc quay lại tài liệu nếu chatbot đang mở, và giữ nguyên trạng thái đóng nếu chatbot đang đóng => xong
- Thiết lập bộ lọc truyền động bật/tắt hiển thị từng loại thực thể (lesson, directory, tag, user) dưới dạng Legend tương tác trên Đồ thị Canvas => xong
- Sửa lỗi nút Quay lại của thực thể ở popup chi tiết khi click không có tác dụng, chuyển hướng đúng về tab Đồ thị => xong
- Tích hợp tính năng mở rộng hiển thị theo số cạnh tùy chọn (1-4 Hops) khi click vào một nút trên đồ thị tri thức, tự động tính toán BFS client-side và tô màu theo dải gradient sắc nét (Hop 0: xanh dương, Hop 1: cyan, Hop 2: xanh lá, Hop 3: vàng, Hop 4: cam), làm mờ các phần tử ngoài tầm hiển thị => xong
- Khắc phục lỗi chatbot bị treo loading spinner hoặc trễ phản hồi trên môi trường deploy đám mây (Render/Vercel) => xong:
    * Backend: Tự động nâng cấp mô hình Gemini (Auto-mapping từ `gemini-1.5-flash`/`gemini-1.5-pro` sang `gemini-2.5-flash`/`gemini-2.5-pro`) để tránh lỗi 404 khi sử dụng API Key mới.
    * Backend: Thay đổi mô hình Embedding mặc định từ `text-embedding-004` thành `gemini-embedding-2` để tránh lỗi 404 và trễ timeout 5 giây.
    * Backend: Thiết lập các response header (`Cache-Control: no-cache` và `X-Accel-Buffering: no`) để vô hiệu hóa tính năng đệm proxy ngược của Render/Nginx, trả stream thời gian thực lập tức.
    * Backend: Tối ưu hóa truy cập database (thêm `prefetch_related('directories')`, `.only(...)` và `.values_list(...)`) để loại bỏ triệt để lỗi N+1 query và giảm dung lượng truyền tải mạng giữa máy chủ Render và cơ sở dữ liệu Supabase/Neon.
    * Frontend: Khai báo React ref `abortControllerRef` chính xác bằng `useRef` tại `ChatbotWorkspace.tsx` để khắc phục lỗi runtime JS console khiến ngắt luồng tương tác của chatbot.
    * Frontend: Tải và trích xuất danh sách lớp học duy nhất (availableClasses) động từ tất cả tài liệu trong cơ sở dữ liệu, hiển thị danh sách lớp gợi ý dưới dạng dropdown Select mode="tags" để giáo viên chọn trực tiếp thay vì tự gõ nhập tay.
    * Backend: Tái cấu trúc logic cập nhật tài liệu (perform_update) và xét duyệt lịch sử (EditHistoryReviewAPIView). Khi người dùng/giáo viên chỉnh sửa tài liệu công khai (PUBLISHED), trạng thái của tài liệu sẽ chuyển sang PENDING (chờ duyệt) và tự động tạo Yêu cầu xét duyệt (ApprovalRequest) và Lịch sử chỉnh sửa (LessonPlanEditHistory) ở trạng thái PENDING.
    * Backend: Thêm cơ chế khôi phục (Rollback) hoàn hảo khi ADMIN từ chối (REJECT) thay đổi: hệ thống sẽ tự động khôi phục các trường dữ liệu của tài liệu (tiêu đề, mô tả, đối tượng, attributes) về giá trị lịch sử trước khi chỉnh sửa (title_before, description_before,...) và đưa trạng thái tài liệu trở lại PUBLISHED. Khi chấp nhận (APPROVE), tài liệu sẽ được phê duyệt và đổi lại trạng thái PUBLISHED.
    * Hệ thống & So sánh Tệp (Word/PDF): Lưu trữ bản sao vật lý của cả tệp tin cũ (`file_path_before`) và tệp tin mới (`file_path_after`) trong cơ sở dữ liệu khi sửa đổi tài liệu. Cho phép Admin/Teacher tải xuống cả hai phiên bản trực tiếp từ giao diện Lịch sử chỉnh sửa hoặc Xét duyệt chỉnh sửa để so sánh và kiểm tra trực quan sự khác biệt của tệp Word. Đồng thời khôi phục lại tệp tin gốc nếu yêu cầu chỉnh sửa bị từ chối.
- Sửa lỗi đăng nhập bằng email hoặc số điện thoại: => xong
- Sửa lỗi hiển thị nút "Thêm mới" tài khoản, hiển thị Logo & Favicon hệ thống: => xong
    * Frontend: Khai báo định nghĩa các màu sắc trung gian tuỳ chỉnh mới (`--color-purple-650`, `--color-blue-650`,...) vào khối `@theme inline` của Tailwind CSS v4 trong tệp `theme.css`. Điều này giải quyết triệt để lỗi thiết lập nền màu trong suốt khiến nút "Thêm mới" và biểu tượng Logo thương hiệu (`bg-blue-650`) bị ẩn hoàn toàn (màu trắng trên nền trắng).
    * Frontend: Thiết lập thư mục `public` cho Vite và thêm tệp tin vector `favicon.svg` chất lượng cao. Đồng thời liên kết thẻ `<link rel="icon" ...>` tương ứng vào `index.html` và tối ưu hoá tiêu đề trang phục vụ SEO tốt nhất.
- Sửa lỗi tự động đồng bộ/chuyển đổi phiên Chatbot khi AI đang phản hồi: => xong
    * Frontend: Cập nhật các hook `useEffect` đồng bộ ngữ cảnh (focusLessonId) và lịch sử phiên (`fetchSessions`) trong `ChatbotWorkspace.tsx`. Khi AI đang phản hồi (`sending` bằng `true`), hệ thống sẽ bỏ qua mọi lệnh tự động chuyển đổi ngữ cảnh chat hoặc tự động tạo phiên mới khi người dùng thoát/mở card giáo án khác. Điều này bảo vệ an toàn cho cuộc trò chuyện hiện tại và câu truy vấn đang được trả lời không bị ngắt quãng vô lý.
- Tái cấu trúc dự án Frontend (Refactor App.tsx + dọn dẹp file): => xong
    * Tách toàn bộ components thành thư mục con (admin/, auth/, chatbot/, directory/, library/, viewer/)
    * Tạo nhánh git feature/refactor-and-fixes, commit 80 files (16.467 dòng thêm, 10.962 dòng xóa)
    * Xóa 32 file không cần thiết: backup, scratch scripts, debug logs, test files, PDF tạm
    * Xóa debug code ghi log tạm thời (api_call_logs.txt writer) trong LessonPlanListAPIView.list()

==============================================================================
ĐÁNH GIÁ TỔNG THỂ HỆ THỐNG - 04/07/2026
==============================================================================

I. TỔNG QUAN HỆ THỐNG
----------------------
Hệ thống Quản lý Tri thức Giáo án (KMS) là nền tảng chia sẻ và khám phá tài
liệu giảng dạy dành cho giáo viên phổ thông, tích hợp AI RAG (Retrieval-
Augmented Generation) cục bộ, đồ thị tri thức tương tác và trợ lý hỏi đáp
thông minh. Hệ thống gồm 2 tầng chính: Backend Django + Frontend React/Vite.

II. ĐÁNH GIÁ TỪNG NHÓM CHỨC NĂNG
-----------------------------------

[✅ HOẠT ĐỘNG TỐT] Xác thực & Phân quyền
  - Đăng nhập bằng username / email / số điện thoại (Q() query LoginAPIView)
  - Đăng ký tài khoản, tìm kiếm tài khoản theo email
  - Khôi phục mật khẩu qua mã OTP (otp_code / otp_created_at trên model User)
  - 3 vai trò: ADMIN / TEACHER / USER với phân quyền khác nhau
  - ADMIN có quyền chỉnh sửa tài liệu của tất cả người dùng
  - Khóa / Mở khóa tài khoản người dùng trực tiếp trên Dashboard Admin
  - Logo thương hiệu và Favicon SVG hiển thị đúng

[✅ HOẠT ĐỘNG TỐT] Quản lý Thư viện & Tài liệu
  - Tải lên file Word (.docx), Markdown (.md), văn bản (.txt)
  - Auto-fill thông tin giáo án từ file (.docx parser bóc tách metadata)
  - Chống trùng lặp thông minh (difflib SequenceMatcher >= 95%)
  - Hiển thị dạng lưới card (Grid) với phân trang 10-20 tài liệu/trang
  - Sắp xếp: mới nhất / đánh giá cao / nhiều lượt đánh giá
  - Bộ lọc đa chiều client-side (Lớp học, Môn học, Đối tượng, Địa điểm,
    Mạch kiến thức, Chủ đề, Kiến thức liên quan)
  - Xem tài liệu dạng Markdown có bảng biểu, tiêu đề, rich format
  - Xem trước file Word (.docx) trực tiếp trên browser (docx-preview)
  - Tải xuống file Word gốc và bản Markdown đã chuyển đổi
  - Cây thư mục đệ quy (public/private), kéo thả, tạo inline, đổi tên

[✅ HOẠT ĐỘNG TỐT] Tìm kiếm
  - Tìm kiếm fuzzy/substring trên tiêu đề, mô tả và nội dung file
  - Xếp hạng độ liên quan: tiêu đề bắt đầu (100) > tiêu đề chứa (50)
    > mô tả (20) > nội dung (10)
  - Bôi sáng từ khóa (highlight) trong card danh sách và trong DetailView
  - Cuộn mượt mà (scrollIntoView smooth) đến đoạn trùng khớp đầu tiên
  - Kết quả tìm kiếm hiển thị snippet nội dung file tại chỗ khớp

[✅ HOẠT ĐỘNG TỐT] Đánh giá & Bình luận
  - Hệ thống đánh giá sao (1-5) cho từng tài liệu
  - Biểu đồ thống kê phân bổ sao (Rating bar chart) tương tác
  - Chỉnh sửa / xóa nhận xét của bản thân inline trực tiếp
  - Bảo mật chống XSS (HTML Escape phía Backend)
  - Cache đánh giá theo session (0 request lần 2)

[✅ HOẠT ĐỘNG TỐT] Sơ đồ Tư duy Tương tác
  - Sơ đồ 4 nhánh đối xứng (Mục tiêu / Thiết bị / Tiến trình / Hoạt động)
  - Tự động zoom về trung tâm mượt mà (fitView 800ms)
  - Nút Reset vị trí & Nút 🎯 Về giữa
  - Bộ giải mã thích ứng (Adaptive Parser) hỗ trợ cả cấu trúc bảng và danh sách

[✅ HOẠT ĐỘNG TỐT] Trợ lý AI RAG (ChatbotWorkspace)
  - Hybrid Search: Vector Search pgvector + FTS Keyword Fallback + Graph BFS
  - Chế độ Focus Mode (bind ngữ cảnh tài liệu đang xem)
  - Nút 🛑 Dừng câu trả lời (AbortController)
  - Nút 🔄 Làm lại câu trước (thu hồi lịch sử + đưa lại câu hỏi cũ)
  - Chỉnh sửa câu hỏi cũ inline (✏️ bút chì), tự cắt lịch sử từ đó
  - Render Markdown phong phú trong chat (tiêu đề, danh sách, bảng biểu)
  - Resize cửa sổ chat (kéo phân cách), lưu độ rộng vào localStorage
  - Sidebar lịch sử ghim/ẩn bằng tab ▶ cạnh trái
  - Bảo toàn ngữ cảnh khi AI đang phản hồi (sending flag guard)
  - Không tạo session trống khi tải lại trang (chỉ tạo khi isOpen + có chat)

[✅ HOẠT ĐỘNG TỐT] Đồ thị Tri thức (Graph Canvas)
  - Vẽ đồ thị 2D Force-Directed (Nodes + Edges)
  - BFS Hop Coloring (1-4 hops, gradient 5 màu)
  - Legend tương tác bật/tắt từng loại thực thể
  - Double-click node mở tài liệu / chọn thư mục
  - Zoom & Pan, chống scroll chaining (overscroll-behavior: contain)
  - Popup thông tin thực thể ghim được, kéo co giãn 4 góc

[✅ HOẠT ĐỘNG TỐT] WikiNotes (Obsidian Vault)
  - Split-pane 35%/65% hiển thị danh sách note và trình đọc
  - Phân tích WikiLinks [[Khái niệm]] → Purple badge tương tác
  - Lọc note theo tài liệu đang focus (API /obsidian/notes/by-lesson/)
  - Định nghĩa khái niệm học thuật tự động bằng LLM

[✅ HOẠT ĐỘNG TỐT] Xử lý AI Ngầm (Background Processor)
  - 5 giai đoạn: Parse Word → Chunking → Embedding → Concept Extraction → Obsidian Sync
  - Progress Roadmap thời gian thực (polling 3 giây)
  - Nút Dừng xử lý (Stop Task) giữa các Phase
  - Beacon API tự dừng khi tắt/thoát trình duyệt
  - Thread Lock bảo vệ GGUF (n_ctx=4096) tránh crash đa luồng

[✅ HOẠT ĐỘNG TỐT] Chỉnh sửa & Duyệt Tài liệu (Edit History Workflow)
  - Giáo viên chỉnh sửa tài liệu PUBLISHED → tự động chuyển sang PENDING
  - Tạo LessonPlanEditHistory lưu bản sao vật lý file cũ/mới (file_path_before/after)
  - Admin duyệt (APPROVE): cập nhật tài liệu + trạng thái PUBLISHED
  - Admin từ chối (REJECT): rollback hoàn toàn về giá trị cũ + PUBLISHED
  - Giao diện xem và tải xuống cả 2 phiên bản file (HistoryModal, ApprovalModal)
  - Chọn lớp học từ danh sách có sẵn trong DB (dropdown, không gõ tay)

[✅ HOẠT ĐỘNG TỐT] Quản trị Admin
  - Dashboard thống kê tổng quan
  - Quản lý tài khoản (tạo, khóa, nâng/hạ vai trò)
  - Phân quyền thư mục đệ quy (2 sub-tab: cá nhân / public)
  - Cấu hình AI Model Engine (3B / 7B / API)
  - Cấu hình chiến lược chia chunk (Heading / Fixed)
  - Duyệt đề xuất công khai tài liệu

III. CÁC VẤN ĐỀ ĐÃ BIẾT / GIỚI HẠN HIỆN TẠI
-----------------------------------------------
  - AI GGUF offline (Qwen 7B) yêu cầu phần cứng mạnh (RAM >= 8GB)
  - Deploy cloud: cần dùng Gemini API thay GGUF cục bộ
  - Chưa có tính năng gợi ý tài liệu tự động (Recommendation Engine)
  - Chưa có thông báo real-time (Notification) khi được duyệt/từ chối
  - Chưa có tính năng chia sẻ link trực tiếp đến tài liệu
  - Chưa có export báo cáo thống kê (PDF/Excel)

IV. KIẾN TRÚC KỸ THUẬT HIỆN TẠI
-----------------------------------
  Backend:  Django 6.0 + DRF + PostgreSQL (pgvector cổng 5433)
  Frontend: React 18 + TypeScript + Vite 6 + Tailwind CSS v4
  AI Local: Qwen 7B GGUF (llama.cpp) + nomic-embed-text (Ollama)
  AI Cloud: Gemini 2.5 Flash/Pro API (fallback tự động)
  Vault:    Obsidian Vault Sync (Markdown note + WikiLinks)
  Auth:     JWT Token (local) + Keycloak SSO (tích hợp sẵn)
  Deploy:   Render (Backend) + Vercel/GitHub Pages (Frontend)

V. TRẠNG THÁI NHÁNH GIT HIỆN TẠI
------------------------------------
  main                       : stable (không thay đổi)
  feature/refactor-and-fixes : active (đang phát triển)
    - perf: optimize concept extraction using intelligent semantic chunk filtering
    - docs: add prompt evaluation report for chatbot and concept extraction
    - docs+fix: system evaluation report + clean debug logging code in views.py
    - chore: remove old App.tsx backups and backend log/evaluation files
    - chore: remove unnecessary files (scratch, debug logs, test files, temp docs, PDF)
    - [Current modifications]:
      * Refactored Word document parser and UploadPage auto-fill engine (`UploadPage.tsx` & `docx_parser.py`): Auto-extract title, subject, grade, duration/periods, teaching location, knowledge track, topic, integrated biology connections tags, and pedagogical execution tables into real-time preview and auto-filled form.
      * Enhanced Inline Detailed Lesson Editing (`DetailView.tsx` & `ApprovalModal.tsx`): Added comprehensive editing fields for metadata (Title, System Directory, Duration/Periods, Target Students, Lesson Type, Location, Knowledge Track, Topic, Multi-tag Class Selector, Summary Description, and Word/PDF attachment file replacement) with seamless PENDING approval & rollback workflows.
      * Fixed Period / Duration Filter logic (`FilterSidebar.tsx`, `LibraryList.tsx` & `useKmsApp.ts`): Standardized filtering by lesson execution period (`Thời gian thực hiện` / `Số tiết`), enabling accurate 1-period, 2-period, and 3+-period filtering in client-side AND/OR filter pipelines.
      * Unified lesson activity data pipeline across Mindmap and DetailView using helper functions (`getLessonMindmapData`, `getLessonActivitiesTimeline`).
      * Expanded Activity Detail Modal dimensions (94vw x 90vh) and table cell rendering with HTML line-break parsing `<br/>`.
      * Redesigned Activity Timeline into a connected vertical blue line diagram with node circles, standardized step labels (`Hoạt động 1`, `Hoạt động 2`...), and duration badges (`⏱️ 10 phút`).
- Sửa lỗi click đúp mở giáo án trên Đồ thị tri thức (Graph Canvas) => xong
- Tích hợp nút Dừng xử lý (Stop Task) cho luồng ngầm AI RAG => xong
- Thiết lập cơ chế Beacon tự động dừng toàn bộ tiến trình khi người dùng tắt/thoát web => xong
- Tách biệt hành vi click card xem chi tiết với nút kích hoạt và liên kết trợ lý AI tập trung, sửa lỗi hộp thoại "Lịch sử trò chuyện" (Chat history dialog) xuất hiện sai ngữ cảnh ở trang chủ => xong
- Sửa lỗi click/double click trên Đồ thị tri thức (Graph Canvas) không mở được giáo án khi giáo án không nằm trong cache local => xong
- Khóa hoàn toàn lỗi Zoom Đồ thị kéo theo Zoom Trang web và loại bỏ hiện tượng cuộn kép (Scroll Chaining) bằng overscroll-behavior: contain => xong

- Thứ 1 là không có 2 file mẫu cho 2 đối tượng học sinh thành thị/nông thôn (chuyên biệt cho từng đối tượng)
- Thứ 2 là do đã thống nhât từ trước là 1 file đưa lên hệ thống sẽ là file KH bài giảng hoàn chỉnh 
    tại vì đầu tiên với nhu cầu là truy vấn tìm kiếm giáo án cho nhanh, tìm kiếm giáo án theo mô tả của ngta thì cần có file KHBG hoàn chỉnh
    việc tụi em làm là đọc file trích xuất thông tin để đáp ứng nhu cầu truy vấn nên file phải chi tiết hóa một chút
    
    Trong trường hợp cô muốn tải 1 file với cấu trúc theo kiểu so sánh giữa 2 đối tượng học sinh thì
        - Nó sẽ giúp ích được gì cho người sử dụng tài liệu của cô?
        - Giả sử cô chỉ đưa ra ý tưởng thì không thể làm được.


- vẽ 1 form cho gv cấp 3 chọn nhiều thứ, đối tượng HS, chọn phòng thiết bị
chọn thời gian, phải vẽ hết (cô nói trong ghi âm) các sự lựa chọn của GV
khi mà họ chọn thì bấm nút tìm nó ra cái gì?
- cô upload từng file phải lấy được nhiều thông tin

- Khóa cứng lỗi zoom đồ thị và zoom toàn trang, đồng thời tích hợp cơ chế ngăn chặn cuộn kép (overscroll-behavior) => xong
- Tránh đóng chatbot tự động khi xem chi tiết giáo án hoặc quay lại để người dùng luôn có thể click Quay lại điều hướng từng bước => xong
- Sửa hành vi double-click vào Thư mục (directory) trên đồ thị để mở/chọn thư mục đó trong cây thư mục thư viện ở trang chủ => xong
- Ghim bảng tóm tắt thực thể/bài giảng/thư mục lệch khỏi nút gốc để tránh che khuất, kiểm tra giới hạn viền đồ thị và hỗ trợ kéo co giãn từ cả 4 góc của bảng, tự động phóng to/thu nhỏ kích cỡ chữ trong popup tương ứng với tỉ lệ co giãn => xong
- Thêm nút 'Quay lại' trên thanh tiêu đề chi tiết giáo án (App.tsx) bên cạnh nút close ✕ để quay lại tài liệu đã xem trước đó theo từng bước mà không cần mở chatbot, tự động lưu trữ ngăn xếp lịch sử xem giáo án => xong
- Sửa lỗi useRef is not defined ở App.tsx bằng cách thêm import useRef từ react => xong
- Đồng bộ tự động phiên chatbot AI khi di chuyển hoặc quay lại tài liệu nếu chatbot đang mở, và giữ nguyên trạng thái đóng nếu chatbot đang đóng => xong
- Thiết lập bộ lọc truyền động bật/tắt hiển thị từng loại thực thể (lesson, directory, tag, user) dưới dạng Legend tương tác trên Đồ thị Canvas => xong
- Sửa lỗi nút Quay lại của thực thể ở popup chi tiết khi click không có tác dụng, chuyển hướng đúng về tab Đồ thị => xong
- Tích hợp tính năng mở rộng hiển thị theo số cạnh tùy chọn (1-4 Hops) khi click vào một nút trên đồ thị tri thức, tự động tính toán BFS client-side và tô màu theo dải gradient sắc nét (Hop 0: xanh dương, Hop 1: cyan, Hop 2: xanh lá, Hop 3: vàng, Hop 4: cam), làm mờ các phần tử ngoài tầm hiển thị => xong
- Khắc phục lỗi chatbot bị treo loading spinner hoặc trễ phản hồi trên môi trường deploy đám mây (Render/Vercel) => xong:
    * Backend: Tự động nâng cấp mô hình Gemini (Auto-mapping từ `gemini-1.5-flash`/`gemini-1.5-pro` sang `gemini-2.5-flash`/`gemini-2.5-pro`) để tránh lỗi 404 khi sử dụng API Key mới.
    * Backend: Thay đổi mô hình Embedding mặc định từ `text-embedding-004` thành `gemini-embedding-2` để tránh lỗi 404 và trễ timeout 5 giây.
    * Backend: Thiết lập các response header (`Cache-Control: no-cache` và `X-Accel-Buffering: no`) để vô hiệu hóa tính năng đệm proxy ngược của Render/Nginx, trả stream thời gian thực lập tức.
    * Backend: Tối ưu hóa truy cập database (thêm `prefetch_related('directories')`, `.only(...)` và `.values_list(...)`) để loại bỏ triệt để lỗi N+1 query và giảm dung lượng truyền tải mạng giữa máy chủ Render và cơ sở dữ liệu Supabase/Neon.
    * Frontend: Khai báo React ref `abortControllerRef` chính xác bằng `useRef` tại `ChatbotWorkspace.tsx` để khắc phục lỗi runtime JS console khiến ngắt luồng tương tác của chatbot.
    * Frontend: Tải và trích xuất danh sách lớp học duy nhất (availableClasses) động từ tất cả tài liệu trong cơ sở dữ liệu, hiển thị danh sách lớp gợi ý dưới dạng dropdown Select mode="tags" để giáo viên chọn trực tiếp thay vì tự gõ nhập tay.
    * Backend: Tái cấu trúc logic cập nhật tài liệu (perform_update) và xét duyệt lịch sử (EditHistoryReviewAPIView). Khi người dùng/giáo viên chỉnh sửa tài liệu công khai (PUBLISHED), trạng thái của tài liệu sẽ chuyển sang PENDING (chờ duyệt) và tự động tạo Yêu cầu xét duyệt (ApprovalRequest) và Lịch sử chỉnh sửa (LessonPlanEditHistory) ở trạng thái PENDING.
    * Backend: Thêm cơ chế khôi phục (Rollback) hoàn hảo khi ADMIN từ chối (REJECT) thay đổi: hệ thống sẽ tự động khôi phục các trường dữ liệu của tài liệu (tiêu đề, mô tả, đối tượng, attributes) về giá trị lịch sử trước khi chỉnh sửa (title_before, description_before,...) và đưa trạng thái tài liệu trở lại PUBLISHED. Khi chấp nhận (APPROVE), tài liệu sẽ được phê duyệt và đổi lại trạng thái PUBLISHED.
    * Hệ thống & So sánh Tệp (Word/PDF): Lưu trữ bản sao vật lý của cả tệp tin cũ (`file_path_before`) và tệp tin mới (`file_path_after`) trong cơ sở dữ liệu khi sửa đổi tài liệu. Cho phép Admin/Teacher tải xuống cả hai phiên bản trực tiếp từ giao diện Lịch sử chỉnh sửa hoặc Xét duyệt chỉnh sửa để so sánh và kiểm tra trực quan sự khác biệt của tệp Word. Đồng thời khôi phục lại tệp tin gốc nếu yêu cầu chỉnh sửa bị từ chối.
- Sửa lỗi đăng nhập bằng email hoặc số điện thoại: => xong
- Sửa lỗi hiển thị nút "Thêm mới" tài khoản, hiển thị Logo & Favicon hệ thống: => xong
    * Frontend: Khai báo định nghĩa các màu sắc trung gian tuỳ chỉnh mới (`--color-purple-650`, `--color-blue-650`,...) vào khối `@theme inline` của Tailwind CSS v4 trong tệp `theme.css`. Điều này giải quyết triệt để lỗi thiết lập nền màu trong suốt khiến nút "Thêm mới" và biểu tượng Logo thương hiệu (`bg-blue-650`) bị ẩn hoàn toàn (màu trắng trên nền trắng).
    * Frontend: Thiết lập thư mục `public` cho Vite và thêm tệp tin vector `favicon.svg` chất lượng cao. Đồng thời liên kết thẻ `<link rel="icon" ...>` tương ứng vào `index.html` và tối ưu hoá tiêu đề trang phục vụ SEO tốt nhất.
- Sửa lỗi tự động đồng bộ/chuyển đổi phiên Chatbot khi AI đang phản hồi: => xong
    * Frontend: Cập nhật các hook `useEffect` đồng bộ ngữ cảnh (focusLessonId) và lịch sử phiên (`fetchSessions`) trong `ChatbotWorkspace.tsx`. Khi AI đang phản hồi (`sending` bằng `true`), hệ thống sẽ bỏ qua mọi lệnh tự động chuyển đổi ngữ cảnh chat hoặc tự động tạo phiên mới khi người dùng thoát/mở card giáo án khác. Điều này bảo vệ an toàn cho cuộc trò chuyện hiện tại và câu truy vấn đang được trả lời không bị ngắt quãng vô lý.
- Tái cấu trúc dự án Frontend (Refactor App.tsx + dọn dẹp file): => xong
    * Tách toàn bộ components thành thư mục con (admin/, auth/, chatbot/, directory/, library/, viewer/)
    * Tạo nhánh git feature/refactor-and-fixes, commit 80 files (16.467 dòng thêm, 10.962 dòng xóa)
    * Xóa 32 file không cần thiết: backup, scratch scripts, debug logs, test files, PDF tạm
    * Xóa debug code ghi log tạm thời (api_call_logs.txt writer) trong LessonPlanListAPIView.list()

==============================================================================
ĐÁNH GIÁ TỔNG THỂ HỆ THỐNG - 04/07/2026
==============================================================================

I. TỔNG QUAN HỆ THỐNG
----------------------
Hệ thống Quản lý Tri thức Giáo án (KMS) là nền tảng chia sẻ và khám phá tài
liệu giảng dạy dành cho giáo viên phổ thông, tích hợp AI RAG (Retrieval-
Augmented Generation) cục bộ, đồ thị tri thức tương tác và trợ lý hỏi đáp
thông minh. Hệ thống gồm 2 tầng chính: Backend Django + Frontend React/Vite.

II. ĐÁNH GIÁ TỪNG NHÓM CHỨC NĂNG
-----------------------------------

[✅ HOẠT ĐỘNG TỐT] Xác thực & Phân quyền
  - Đăng nhập bằng username / email / số điện thoại (Q() query LoginAPIView)
  - Đăng ký tài khoản, tìm kiếm tài khoản theo email
  - Khôi phục mật khẩu qua mã OTP (otp_code / otp_created_at trên model User)
  - 3 vai trò: ADMIN / TEACHER / USER với phân quyền khác nhau
  - ADMIN có quyền chỉnh sửa tài liệu của tất cả người dùng
  - Khóa / Mở khóa tài khoản người dùng trực tiếp trên Dashboard Admin
  - Logo thương hiệu và Favicon SVG hiển thị đúng

[✅ HOẠT ĐỘNG TỐT] Quản lý Thư viện & Tài liệu
  - Tải lên file Word (.docx), Markdown (.md), văn bản (.txt)
  - Auto-fill thông tin giáo án từ file (.docx parser bóc tách metadata)
  - Chống trùng lặp thông minh (difflib SequenceMatcher >= 95%)
  - Hiển thị dạng lưới card (Grid) với phân trang 10-20 tài liệu/trang
  - Sắp xếp: mới nhất / đánh giá cao / nhiều lượt đánh giá
  - Bộ lọc đa chiều client-side (Lớp học, Môn học, Đối tượng, Địa điểm,
    Mạch kiến thức, Chủ đề, Kiến thức liên quan)
  - Xem tài liệu dạng Markdown có bảng biểu, tiêu đề, rich format
  - Xem trước file Word (.docx) trực tiếp trên browser (docx-preview)
  - Tải xuống file Word gốc và bản Markdown đã chuyển đổi
  - Cây thư mục đệ quy (public/private), kéo thả, tạo inline, đổi tên

[✅ HOẠT ĐỘNG TỐT] Tìm kiếm
  - Tìm kiếm fuzzy/substring trên tiêu đề, mô tả và nội dung file
  - Xếp hạng độ liên quan: tiêu đề bắt đầu (100) > tiêu đề chứa (50)
    > mô tả (20) > nội dung (10)
  - Bôi sáng từ khóa (highlight) trong card danh sách và trong DetailView
  - Cuộn mượt mà (scrollIntoView smooth) đến đoạn trùng khớp đầu tiên
  - Kết quả tìm kiếm hiển thị snippet nội dung file tại chỗ khớp

[✅ HOẠT ĐỘNG TỐT] Đánh giá & Bình luận
  - Hệ thống đánh giá sao (1-5) cho từng tài liệu
  - Biểu đồ thống kê phân bổ sao (Rating bar chart) tương tác
  - Chỉnh sửa / xóa nhận xét của bản thân inline trực tiếp
  - Bảo mật chống XSS (HTML Escape phía Backend)
  - Cache đánh giá theo session (0 request lần 2)

[✅ HOẠT ĐỘNG TỐT] Sơ đồ Tư duy Tương tác
  - Sơ đồ 4 nhánh đối xứng (Mục tiêu / Thiết bị / Tiến trình / Hoạt động)
  - Tự động zoom về trung tâm mượt mà (fitView 800ms)
  - Nút Reset vị trí & Nút 🎯 Về giữa
  - Bộ giải mã thích ứng (Adaptive Parser) hỗ trợ cả cấu trúc bảng và danh sách

[✅ HOẠT ĐỘNG TỐT] Trợ lý AI RAG (ChatbotWorkspace)
  - Hybrid Search: Vector Search pgvector + FTS Keyword Fallback + Graph BFS
  - Chế độ Focus Mode (bind ngữ cảnh tài liệu đang xem)
  - Nút 🛑 Dừng câu trả lời (AbortController)
  - Nút 🔄 Làm lại câu trước (thu hồi lịch sử + đưa lại câu hỏi cũ)
  - Chỉnh sửa câu hỏi cũ inline (✏️ bút chì), tự cắt lịch sử từ đó
  - Render Markdown phong phú trong chat (tiêu đề, danh sách, bảng biểu)
  - Resize cửa sổ chat (kéo phân cách), lưu độ rộng vào localStorage
  - Sidebar lịch sử ghim/ẩn bằng tab ▶ cạnh trái
  - Bảo toàn ngữ cảnh khi AI đang phản hồi (sending flag guard)
  - Không tạo session trống khi tải lại trang (chỉ tạo khi isOpen + có chat)

[✅ HOẠT ĐỘNG TỐT] Đồ thị Tri thức (Graph Canvas)
  - Vẽ đồ thị 2D Force-Directed (Nodes + Edges)
  - BFS Hop Coloring (1-4 hops, gradient 5 màu)
  - Legend tương tác bật/tắt từng loại thực thể
  - Double-click node mở tài liệu / chọn thư mục
  - Zoom & Pan, chống scroll chaining (overscroll-behavior: contain)
  - Popup thông tin thực thể ghim được, kéo co giãn 4 góc

[✅ HOẠT ĐỘNG TỐT] WikiNotes (Obsidian Vault)
  - Split-pane 35%/65% hiển thị danh sách note và trình đọc
  - Phân tích WikiLinks [[Khái niệm]] → Purple badge tương tác
  - Lọc note theo tài liệu đang focus (API /obsidian/notes/by-lesson/)
  - Định nghĩa khái niệm học thuật tự động bằng LLM

[✅ HOẠT ĐỘNG TỐT] Xử lý AI Ngầm (Background Processor)
  - 5 giai đoạn: Parse Word → Chunking → Embedding → Concept Extraction → Obsidian Sync
  - Progress Roadmap thời gian thực (polling 3 giây)
  - Nút Dừng xử lý (Stop Task) giữa các Phase
  - Beacon API tự dừng khi tắt/thoát trình duyệt
  - Thread Lock bảo vệ GGUF (n_ctx=4096) tránh crash đa luồng

[✅ HOẠT ĐỘNG TỐT] Chỉnh sửa & Duyệt Tài liệu (Edit History Workflow)
  - Giáo viên chỉnh sửa tài liệu PUBLISHED → tự động chuyển sang PENDING
  - Tạo LessonPlanEditHistory lưu bản sao vật lý file cũ/mới (file_path_before/after)
  - Admin duyệt (APPROVE): cập nhật tài liệu + trạng thái PUBLISHED
  - Admin từ chối (REJECT): rollback hoàn toàn về giá trị cũ + PUBLISHED
  - Giao diện xem và tải xuống cả 2 phiên bản file (HistoryModal, ApprovalModal)
  - Chọn lớp học từ danh sách có sẵn trong DB (dropdown, không gõ tay)

[✅ HOẠT ĐỘNG TỐT] Quản trị Admin
  - Dashboard thống kê tổng quan
  - Quản lý tài khoản (tạo, khóa, nâng/hạ vai trò)
  - Phân quyền thư mục đệ quy (2 sub-tab: cá nhân / public)
  - Cấu hình AI Model Engine (3B / 7B / API)
  - Cấu hình chiến lược chia chunk (Heading / Fixed)
  - Duyệt đề xuất công khai tài liệu

III. CÁC VẤN ĐỀ ĐÃ BIẾT / GIỚI HẠN HIỆN TẠI
-----------------------------------------------
  - AI GGUF offline (Qwen 7B) yêu cầu phần cứng mạnh (RAM >= 8GB)
  - Deploy cloud: cần dùng Gemini API thay GGUF cục bộ
  - Chưa có tính năng gợi ý tài liệu tự động (Recommendation Engine)
  - Chưa có thông báo real-time (Notification) khi được duyệt/từ chối
  - Chưa có tính năng chia sẻ link trực tiếp đến tài liệu
  - Chưa có export báo cáo thống kê (PDF/Excel)

IV. KIẾN TRÚC KỸ THUẬT HIỆN TẠI
-----------------------------------
  Backend:  Django 6.0 + DRF + PostgreSQL (pgvector cổng 5433)
  Frontend: React 18 + TypeScript + Vite 6 + Tailwind CSS v4
  AI Local: Qwen 7B GGUF (llama.cpp) + nomic-embed-text (Ollama)
  AI Cloud: Gemini 2.5 Flash/Pro API (fallback tự động)
  Vault:    Obsidian Vault Sync (Markdown note + WikiLinks)
  Auth:     JWT Token (local) + Keycloak SSO (tích hợp sẵn)
  Deploy:   Render (Backend) + Vercel/GitHub Pages (Frontend)

V. TRẠNG THÁI NHÁNH GIT HIỆN TẠI
------------------------------------
  main                       : stable (không thay đổi)
  feature/refactor-and-fixes : active (đang phát triển)
    - perf: optimize concept extraction using intelligent semantic chunk filtering
    - docs: add prompt evaluation report for chatbot and concept extraction
    - docs+fix: system evaluation report + clean debug logging code in views.py
    - chore: remove old App.tsx backups and backend log/evaluation files
    - chore: remove unnecessary files (scratch, debug logs, test files, temp docs, PDF)
    - [Current modifications]:
      * Refactored Word document parser and UploadPage auto-fill engine (`UploadPage.tsx` & `docx_parser.py`): Auto-extract title, subject, grade, duration/periods, teaching location, knowledge track, topic, integrated biology connections tags, and pedagogical execution tables into real-time preview and auto-filled form.
      * Enhanced Inline Detailed Lesson Editing (`DetailView.tsx` & `ApprovalModal.tsx`): Added comprehensive editing fields for metadata (Title, System Directory, Duration/Periods, Target Students, Lesson Type, Location, Knowledge Track, Topic, Multi-tag Class Selector, Summary Description, and Word/PDF attachment file replacement) with seamless PENDING approval & rollback workflows.
      * Fixed Period / Duration Filter logic (`FilterSidebar.tsx`, `LibraryList.tsx` & `useKmsApp.ts`): Standardized filtering by lesson execution period (`Thời gian thực hiện` / `Số tiết`), enabling accurate 1-period, 2-period, and 3+-period filtering in client-side AND/OR filter pipelines.
      * Unified lesson activity data pipeline across Mindmap and DetailView using helper functions (`getLessonMindmapData`, `getLessonActivitiesTimeline`).
      * Expanded Activity Detail Modal dimensions (94vw x 90vh) and table cell rendering with HTML line-break parsing `<br/>`.
      * Redesigned Activity Timeline into a connected vertical blue line diagram with node circles, standardized step labels (`Hoạt động 1`, `Hoạt động 2`...), and duration badges (`⏱️ 10 phút`).
      * Optimized Mobile/Portrait responsive order: Metadata -> Activity Timeline -> Document Preview -> Pedagogical Reviews at the end.
      * Redesigned sidebar toggle button into a sleek circular icon button `[|<]` / `[|>]` placed at the far right end of the top navbar.
      * Redesigned metadata header card: prominent color-coded badges, combined System Directory and Subject/Grade into 1 block, and expanded space for Integrated Knowledge tags (`lg:grid-cols-[1fr_1.1fr_1.5fr]`).
      * Fixed Docx Parser & Frontend Subject extraction: Robustly extract full Subject name (e.g. "Hoạt động trải nghiệm hướng nghiệp") from docx title block matching "Môn: ...; lớp: ..." without truncation.
      * Upgraded Root Ancestor Track Mapping (`getTrackAndTopicFromDir`): Automatically trace selected directory up to the top-most Root Directory (1 of the 4 main tracks: Bản thân, Xã hội, Tự nhiên, Hướng nghiệp) and map seamlessly to Mạch kiến thức field in UploadPage and DetailView.
      * Enhanced Activity Detail Modal & Truncation Cutoff: Improved dynamic section cutoff regex for teaching activities, eliminating trailing non-activity sections (e.g. V. MỞ RỘNG SAU BÀI HỌC, IV. ĐÁNH GIÁ, PHỤ LỤC...) and upgraded Modal UI with flat single-Select inputs for Subject and Target Students.
      * Refined Activity Block Indexing & Heading Splitter (`DetailView.tsx`): Built targeted `chiTietBlocksMap` with strict Heading 3 marker regex (`### Hoạt động 01:`, `### Hoạt động 02:`...), preventing content leaks and ensuring accurate 1-to-1 mapping for all teaching activities without sequence distortion or cutoff artifacts.
      * Fast Intent Classifier (<50ms): Added immediate streaming response for greetings ("chào bạn", "hi chatbot") and system info queries ("hệ thống này là gì", "giới thiệu về hệ thống") in `views.py` avoiding heavy RAG/LLM calculation.
      * RRF Re-ranking Engine: Upgraded Graph RAG vector retrieval in `graph_rag_service.py` with `pgvector.django.CosineDistance` and Reciprocal Rank Fusion ($k=60$) combining keyword and vector distance search.
      * Enhanced Core Entity Extraction & Chunking: Constrained concept extraction prompt in `bg_processor.py` to 5-8 core academic entities directly present in lesson plans, and truncated `DocumentChunk.heading` to 250 characters max to prevent DB truncation errors.
      * Admin RAG & Reprocess Combobox (`SettingsTab.tsx` & `bg_processor.py`): Integrated real-time failed lessons tracker (`failed_lessons`) and a searchable `ReprocessCombobox` allowing Admins to clear RAG cache & re-process any specific lesson plan.
      * 3-Tab Wiki Notes Viewer (`WikiNotesTab.tsx` & `ChatbotWorkspace.tsx`): Created 3 distinct sub-tabs (`Bài giảng này`, `Chọn bài giảng`, `Toàn hệ thống`) with a dedicated `LessonSearchCombobox` enabling real-time lesson selection & concept entity filtering with disabled state safeguards.
